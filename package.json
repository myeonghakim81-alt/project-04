{
  "name": "tank-arcade",
  "version": "0.17.0",
  "description": "TANKTANK - HTML5 Canvas arcade game with EN/KO/ZH/JA/ES/FR support (web + Capacitor Android wrapper)",
  "private": true,
  "scripts": {
    "serve": "http-server . -p 8080",
    "sync:www": "node scripts/sync-www.js",
    "cap:sync": "npm run sync:www && cap sync android",
    "cap:open": "npm run sync:www && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.1"
  }
}
